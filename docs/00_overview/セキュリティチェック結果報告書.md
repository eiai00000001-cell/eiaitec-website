# セキュリティチェック結果報告書

## 1. 文書情報
- 作成日: 2026-09-09
- 最終更新: 2026-09-09(v1.1)
- 版数: v1.1
- 実施者:
  - v1.0: オーケストレーター(security-expertサブエージェント準備中のため、暫定的に直接実施)
  - v1.1: security-expert(専門サブエージェントによる正式チェック。v1.0の検証・補強)
- 対象: `docs/`配下全体、`mockup/`、`README.md`、`images/`、`.gitignore`、ルート直下の全ファイル、およびGit履歴・リモート公開状態

> 本書は第2章〜第6章にv1.0(暫定対応)の記録を履歴としてそのまま残し、第7章以降にv1.1(security-expertによる正式チェック)の結果を記載する。**最新の結論は第7章以降を参照すること。**

---

# 【v1.0】オーケストレーターによる暫定対応の記録

## 2. 経緯
ユーザーより、`docs/01_requirements/要件定義書.md`・`機能仕様書.md`にお問い合わせフォームの受信先メールアドレス(個人のメールアドレス)が直書きされており、GitHub上で公開された場合に個人情報が漏洩する懸念があるとの指摘を受けた。あわせて、今後はこの種のチェックを専門に行う`security-expert`サブエージェントを`.claude/agents`に追加した(`/Users/nodaeiji/Projects/.claude/agents/security-expert.md`、model: opus)。

## 3. スキャン結果

### 3.1 発見事項

| No. | 重大度 | 該当ファイル | 内容 | Git状態 |
|---|---|---|---|---|
| 1 | 要対応 | `docs/01_requirements/要件定義書.md`(4箇所) | 受信先メールアドレス(個人情報)の直書き | 未追跡(`git status`で`??`。コミット履歴には未混入) |
| 2 | 要対応 | `docs/01_requirements/機能仕様書.md`(3箇所) | 同上 | 未追跡(同上) |

上記以外(`mockup/index.html`、`README.md`、`docs/00_overview/site-direction.md`、`docs/00_overview/プロジェクト計画書.md`、`docs/00_overview/進行モード.md`)には、氏名・電話番号・住所・APIキー・トークン等の個人情報・秘密情報の直書きは確認されなかった。

### 3.2 リスク評価
- いずれの箇所も現時点で`git add`・コミットされていない(untracked)ため、**Gitの履歴には混入していない**。今回の対応で実害は発生していない。
- ただし、今後`git add .`等でコミット・pushされた場合、公開リポジトリ経由で個人のメールアドレスが第三者に閲覧可能になるリスクがあった。

## 4. 実施した是正内容
- `docs/01_requirements/要件定義書.md`・`機能仕様書.md`内の実メールアドレス直書き箇所(計7箇所)を、すべて`[CONTACT_RECEIVE_EMAIL]`というプレースホルダー表記に置換した。
- 各置換箇所に、実アドレスは個人情報のためドキュメントに記載しない旨、および実装フェーズでは`.env.local`等Git追跡対象外ファイルの環境変数として管理する旨の注記を追加した。
- 要件定義書10章に要確認事項No.14として、実装フェーズでの環境変数設定(受信先メールアドレスの`.env.local`化)を申し送り事項として追記した。
- 両ドキュメントの変更履歴にv1.2として本対応を記録した。

## 5. 今後の申し送り事項
- `.gitignore`には`.env` / `.env.local` / `.env.*.local`が既に登録済みであり、実装フェーズで環境変数ファイルを使う運用に問題はない。
- 実装フェーズ(developer)着手時、Resend連携の受信先メールアドレスは`.env.local`等に`CONTACT_RECEIVE_EMAIL`のような環境変数名で設定し、コード内・コミット対象ファイルに直書きしないこと。
- 今後、設計書・実装コード・READMEなど新たな成果物が増えるたびに、コミット前に同様の観点(PII・秘密情報の直書きチェック)を確認することを推奨する。次回セッション以降は`security-expert`サブエージェントが利用可能になるため、そちらに委譲する。
- お問い合わせフォームの入力内容(訪問者が入力する会社名・氏名・メールアドレス等)自体は訪問者本人が任意に送信するものであり、本チェックの対象(開発側が管理するドキュメント・コードへの直書き)とは性質が異なるため対象外とした。

---

# 【v1.1】security-expertによる正式チェック結果

## 6. チェック概要(v1.1)

### 6.1 実施範囲・手法
| 観点 | 実施内容 |
|---|---|
| ワーキングツリー全文スキャン | `docs/`配下全ドキュメント、`mockup/index.html`、`README.md`、`.gitignore`、ルート直下ファイルに対し、メールアドレス・電話番号・住所/郵便番号・APIキー・トークン・パスワード・接続文字列の正規表現スキャンを実施。`mockup/index.html`はbase64画像(約10MB)を除去した上で本文を精査。 |
| 既知シークレット形式の照合 | `re_`(Resend) / `sk-` / `ghp_`等 / `AKIA` / `xox`系 / `AIza` / JWT(`eyJ`)のプレフィックス照合を全ファイルに実施。 |
| Git履歴スキャン | 全ブランチ(`develop` / `main` / `origin/*`)の全コミット差分に対し、追加行ベースでPII・秘密情報を検索。過去に存在し削除済みのファイルも追跡。 |
| Gitメタデータ検査 | `git config user.*`、全コミットのauthor/committer情報を検査。 |
| リモート公開状態の確認 | `git remote -v`およびGitHub公開APIにより、リモートリポジトリの公開/非公開を確認。 |
| バイナリ資産のメタデータ検査 | `images/*.png`(全5点)の埋め込みメタデータ(C2PA / XMP / EXIF相当)に作成者名・アカウント識別子等が含まれないか検査。 |
| `.gitignore`有効性検証 | 実装フェーズで想定される機微ファイル名22パターンに対し`git check-ignore`で実効性を検証。 |

### 6.2 前提となる重要事実
**本リポジトリのGitHubリモートは「public(公開)」である。**

- リモート: `git@github.com:eiai00000001-cell/eiaitec-website.git`
- 公開状態: GitHub API応答にて `"private": false` / `"visibility": "public"` を確認済み。
- したがって、**pushされた時点の全コミット内容およびコミットメタデータは、認証なしで全世界から閲覧・クローン可能**である。この前提はv1.0のリスク評価では明示されていなかったが、以降の重大度判定の根拠となる。

## 7. v1.0(暫定対応)の検証結果

### 7.1 プレースホルダー化の妥当性: 適切(是正済みと確認)
再スキャンの結果、`docs/01_requirements/要件定義書.md`・`機能仕様書.md`の両ファイルに実メールアドレスの残存は**確認されなかった**。置換漏れはない。

- 要件定義書: 6.7節(2箇所)・9章・10章No.1・No.2・No.14で`[CONTACT_RECEIVE_EMAIL]`表記に統一されていることを確認。
- 機能仕様書: 4章F-07(2箇所)・5章データ項目定義で同様に統一されていることを確認。
- 各箇所に「実アドレスは個人情報のため非記載」「実装フェーズでは`.env.local`等で環境変数管理」の注記が付与されており、後続フェーズ(developer)への申し送りとして十分な粒度である。
- 両ドキュメントの変更履歴にv1.2として記録されており、追跡可能性も確保されている。

**評価: v1.0の是正内容は適切であり、追加の修正は不要。**

### 7.2 誤検知として除外した項目(指摘対象外)
以下はスキャンで検出されたが、実在しないダミー値・公開情報であるため指摘対象外とした。

| 検出値 | 該当箇所 | 除外理由 |
|---|---|---|
| `sample@example.com` | `mockup/index.html` 598行目、`機能仕様書.md`(2箇所) | RFC 2606で予約されたドキュメント用ドメイン。実在しない。 |
| `例)栄愛 太郎` | `mockup/index.html`、`機能仕様書.md` | フォームのプレースホルダー用ダミー氏名。実在の個人を指さない。 |
| `例)⚪︎⚪︎商店` | 同上 | 同上(ダミー社名)。 |
| `onboarding@resend.dev` | `要件定義書.md`、`機能仕様書.md` | Resendが公式に提供する検証用共有アドレス。秘密情報ではなく公開ドキュメント記載値。 |
| `eiaitec.jp` | 各所 | 公開予定の事業用ドメイン名。公開前提の情報であり秘密情報ではない。 |
| `fonts.googleapis.com` | `mockup/index.html` | 外部フォントCDN。認証情報を含まない公開URL。 |

### 7.3 v1.0の記述で補正が必要な点
- v1.0第5章に「`.gitignore`には`.env` / `.env.local` / `.env.*.local`が既に登録済みであり、実装フェーズで環境変数ファイルを使う運用に問題はない」とあるが、**この評価は不十分**であった。詳細は9章(指摘No.3)を参照。
- v1.0第3.2章のリスク評価は「未追跡のためGit履歴に混入していない」としており、対象ファイル単体としては正しい。ただし、**同種のPII(個人のメールアドレス)が別経路ですでに公開済みである**点が見落とされていた。詳細は9章(指摘No.1)を参照。

## 8. 新規スキャン結果サマリ

| 対象 | 結果 |
|---|---|
| `docs/00_overview/site-direction.md` | PII・秘密情報なし。方針上、代表者個人の氏名・顔写真・経歴は非公開と明記されており、記述内容も方針と整合。 |
| `docs/00_overview/プロジェクト計画書.md` | PII・秘密情報なし。 |
| `docs/00_overview/進行モード.md` | PII・秘密情報なし。 |
| `docs/01_requirements/要件定義書.md` | 実PIIなし(7.1参照)。 |
| `docs/01_requirements/機能仕様書.md` | 実PIIなし(7.1参照)。 |
| `README.md` | PII・秘密情報なし。 |
| `mockup/index.html` | 本文にPII・秘密情報なし。APIキー・トークン類なし。フォームは`onsubmit="return false;"`のダミー実装で、送信先アドレスや認証情報の埋め込みもなし。 |
| `images/*.png`(5点) | 画像バイナリのメタデータに氏名・メールアドレス・アカウントID等の**個人識別情報は含まれていない**(詳細は9章 指摘No.4)。 |
| `.gitignore` | 不備あり(9章 指摘No.3)。**是正実施済み**。 |
| Git履歴(全ブランチ) | ドキュメント・HTML差分にPII・秘密情報の混入なし。ただしバイナリ1件とコミットメタデータに問題あり(9章 指摘No.1・No.2)。 |
| 既知シークレット形式 | 全ファイルで検出ゼロ。APIキー・トークン・パスワード・接続文字列の直書きは一切確認されなかった。 |

## 9. v1.1 新規発見事項

### 指摘No.1 【緊急】コミットメタデータに個人のメールアドレスが含まれ、公開GitHub上ですでに閲覧可能

| 項目 | 内容 |
|---|---|
| 重大度 | **緊急** |
| 該当箇所 | 全8コミット(`05b6fd7`〜`f6c78c4`)のauthor/committerフィールド、およびローカル`git config user.email` |
| 種別 | 個人情報(個人のメールアドレス) |
| Git状態 | **コミット済み・push済み・公開中**(`origin/main`・`origin/develop`の全コミット) |

**内容**

ローカルGit設定の`user.email`に個人のメールアドレスが設定されており(実値は本書には記載しない)、本リポジトリの**全コミットのauthor/committer情報として記録されている**。リモートはpublicであるため、以下の経路で誰でも取得可能な状態にある。

- GitHubのコミット一覧・各コミット詳細ページ(`.patch` / `.diff` 形式で末尾にアドレスが露出)
- GitHub公開REST API(`GET /repos/{owner}/{repo}/commits`。認証不要でauthor.emailが返る)
- `git clone`によるリポジトリ全体の取得

本チェックにおいて、公開API経由で実際に当該アドレスが取得可能であることを**再現確認済み**である。

**リスク**

- v1.0で「ドキュメントに個人のメールアドレスを書かない」という是正を行った目的(公開リポジトリ経由でのPII漏洩防止)が、**この経路によって実質的に無効化されている**。ドキュメントから除去しても、同一の(または同系統の)個人アドレスがコミットメタデータ経由で公開され続けている。
- 加えて、公開されているGitHubアカウント情報と組み合わせることで、個人の特定が容易になる可能性がある。
- スパム・フィッシング・クレデンシャルスタッフィングの標的リスト化に利用されうる。特に、当該アドレスがお問い合わせフォームの受信先と同一である場合、サイト公開後の到達経路が二重に露出することになる。

**履歴に残っている旨の明記(重要)**

本件は**すでにpush済みであり、以下の点に留意が必要**である。

- 今後のコミット設定を変えても、**過去8コミットのメタデータは書き換わらない**。
- 履歴書き換え(`git filter-repo`等)を行い強制pushしたとしても、**GitHub側に残るキャッシュ・既存のフォーク/クローン・GitHub Archive等のミラー、および第三者が既に取得した複製からは削除できない**。「一度公開されたものは取り消せない」という前提で影響を評価する必要がある。
- したがって本指摘は「完全な消去」ではなく、**①今後の露出停止 ②既存露出の影響低減**の2段構えで対応すべきものである。

**是正案(いずれも本書作成者は未実施。実施可否の判断が必要)**

1. **【推奨・最優先/低リスク】今後のコミットの露出を止める**
   - GitHubの `Settings → Emails` で「Keep my email addresses private」および「Block command line pushes that expose my email」を有効化する。
   - 本リポジトリのローカル設定を、GitHubが払い出すnoreplyアドレスへ変更する。
     ```
     git config user.email "<ID>+<username>@users.noreply.github.com"
     ```
   - グローバル設定(`git config --global user.email`)も同様に変更し、他案件での再発を防ぐ。
2. **【判断が必要/破壊的】過去履歴の書き換え**
   - `git filter-repo --mailmap` 等でauthor/committerを一括置換し、`--force`でpushし直す。
   - **ただし前述のとおり完全な消去にはならず**、加えてコミットSHAが全て変わるためクローン済み環境との整合が壊れる破壊的操作である。**実施する場合はユーザーの明示的な許可が必須。**
   - 本リポジトリは公開直後・関係者1名・フォーク0件と推定されるため、書き換えのコストは比較的低い。実施するなら早いほど効果が高い。
3. **【代替案/最も確実】リポジトリの作り直し**
   - 現行リポジトリを削除(またはprivate化)し、noreplyアドレス設定済みの状態で新規リポジトリとして初期コミットからやり直す。メタ事例としての「開発プロセスの公開」を重視する方針(site-direction.md §2)と両立させるなら、公開する履歴を意図的に作り直す本案が最も安全である。
4. **【方針判断】そもそもpublicである必要があるかの再検討**
   - 「このサイト自体をAI駆動開発のメタ事例として公開する」方針(site-direction.md §2)上、リポジトリ公開には積極的な意義がある。ただしその場合、**リポジトリの全内容が公開前提の成果物である**という認識を全フェーズで徹底する必要がある。公開はデプロイ直前まで待ち、内容を精査してから公開に切り替える運用も選択肢となる。

---

### 指摘No.2 【要対応】`.DS_Store`が過去コミットに混入し、公開リモートに残存

| 項目 | 内容 |
|---|---|
| 重大度 | **要対応** |
| 該当箇所 | コミット `f2101e2`(`fix: update Hero section in website mock`)で追加、`1b364d8`(`docs: update README and .gitignore`)で削除 |
| 種別 | ローカル環境情報の意図せぬ露出(macOS Finderメタデータ) |
| Git状態 | **ワーキングツリー・最新コミットからは削除済み。ただし履歴には残存し、`origin/develop`へpush済み(公開中)** |

**内容**

macOSのFinderが生成する`.DS_Store`(6,148バイト)が`f2101e2`で誤ってコミットされていた。`1b364d8`で`.gitignore`への追加とあわせて削除されているが、**Gitの性質上、削除しても過去コミットのオブジェクトとして履歴に残り続ける**。`f2101e2`は`origin/develop`から到達可能であり、`git show f2101e2:.DS_Store`で誰でも取得できる。

**リスク**

- `.DS_Store`にはFinderの表示設定に加え、**そのディレクトリに当時存在したファイル・フォルダ名(Gitにコミットしていないものを含む)**が記録される。本件の該当ファイルを解析した結果、記録されていたのは`README.md`のみで、**未公開の機密ファイル名や個人情報の露出は確認されなかった**。実害は限定的である。
- ただし一般論として、`.DS_Store`は「コミットすべきでないローカル環境ファイルが混入した」ことを示す兆候であり、同種の事故が`.env`等で起きた場合は重大な結果を招く。今回は`.env`系ではなかったが、**運用上のプロセス不備の指標**として扱うべきである。

**是正案**

- `.gitignore`への`.DS_Store`登録は`1b364d8`で既に実施済みであり、現在のワーキングツリーの`.DS_Store`が追跡対象外であることを`git check-ignore`で確認済み。**今後の再発防止措置は完了している。**
- 履歴からの完全除去には`git filter-repo --path .DS_Store --invert-paths`等の履歴書き換えが必要だが、内容の機微度が低いことから、**指摘No.1の履歴書き換えを実施する場合に同時に処理するのが合理的**である。単独での書き換えは費用対効果が低い。
- 併せて、macOS環境ではグローバルgitignore(`git config --global core.excludesfile`)に`.DS_Store`を登録し、全リポジトリで再発を防ぐことを推奨する。

---

### 指摘No.3 【要対応→是正済み】`.gitignore`が実装フェーズの機微ファイルを十分にカバーしていない

| 項目 | 内容 |
|---|---|
| 重大度 | **要対応**(本チェックにて是正実施済み) |
| 該当箇所 | `.gitignore` |
| 種別 | 秘密情報の誤コミットを防ぐ予防措置の不備 |
| Git状態 | 追跡対象ファイル(コミット済み)。今回の修正は未コミット |

**内容**

v1.0では「`.env` / `.env.local` / `.env.*.local`が登録済みのため問題なし」と評価されていたが、実効性を`git check-ignore`で検証したところ、**実装フェーズで現実に発生しうる複数の機微ファイルが追跡対象になりうる状態**であった。

修正前の検証結果(NOT-IGNORED = コミット可能な状態):

| ファイル名 | 修正前 | 想定される混入内容 |
|---|---|---|
| `.env.production` | NOT-IGNORED | 本番用Resend APIキー・受信先メールアドレス |
| `.env.development` | NOT-IGNORED | 開発用APIキー |
| `.env.test` | NOT-IGNORED | テスト用APIキー |
| `.env.vault` | NOT-IGNORED | 暗号化環境変数 |
| `*.pem` / `*.key` / `*.p12` | NOT-IGNORED | 秘密鍵・証明書 |
| `id_rsa` / `id_ed25519` | NOT-IGNORED | SSH秘密鍵 |
| `secrets.json` / `*credentials*.json` | NOT-IGNORED | 各種認証情報 |
| `*.log`(`npm-debug.log`等以外) | NOT-IGNORED | ログに出力されたトークン・PII |
| `coverage/` | NOT-IGNORED | テスト成果物(機微ではないが不要) |
| `.claude/settings.local.json` | NOT-IGNORED | ローカルツール設定 |

特に`.env.production`は、Next.js/Vercel構成において**Resend APIキーが格納される蓋然性が最も高いファイル名の一つ**であり、これが追跡対象になっている状態は実装フェーズ開始前に必ず解消すべきものであった。

**実施した是正(本チェックで対応済み)**

`.gitignore`を以下のとおり更新した。

- 環境変数: `.env` / `.env.*` を包括的に除外し、値を含まないテンプレートである`.env.example`のみ`!.env.example`で明示的に許可する方式へ変更(ホワイトリスト方式)。`.env.vault`も追加。
- 認証情報・鍵: `*.pem` / `*.key` / `*.p12` / `*.pfx` / `id_rsa*` / `id_ed25519*` / `*credentials*.json` / `*service-account*.json` / `secrets.json` / `secrets.yml` / `secrets.yaml` を追加。
- ログ: `*.log` を追加(トークンやPIIがログ出力される事故への備え)。
- その他: `coverage/` / `.nyc_output/` / `.claude/settings.local.json` / `.turbo/` を追加。macOS関連に`.AppleDouble` / `.LSOverride` / `._*` を追加。

**是正後、上記全パターンがIGNOREDとなり、`.env.example`のみ意図どおり追跡可能であることを`git check-ignore`で再検証済み。** また、既存の追跡対象ファイル(`README.md`・`mockup/index.html`・`images/*.png`)が誤って除外されないことも確認済み。

なお、`.gitignore`はあくまで**未追跡ファイルにのみ有効**であり、一度`git add`されたファイルには効かない。実装フェーズでは`.gitignore`任せにせず、コミット前に`git status`で内容を確認する運用を併用すること。

---

### 指摘No.4 【参考】画像ファイルにAI生成を示すC2PAメタデータが埋め込まれている

| 項目 | 内容 |
|---|---|
| 重大度 | **参考**(PII・秘密情報の観点では問題なし) |
| 該当箇所 | `images/icon.png`、`work.png`、`worker1.png`〜`worker3.png`(全5点)、および`mockup/index.html`に埋め込まれたbase64画像 |
| 種別 | バイナリ埋め込みメタデータ |
| Git状態 | コミット済み・push済み(公開中) |

**内容**

全画像ファイルにC2PA(Content Credentials)の来歴情報が埋め込まれている。内容を解析した結果、含まれていたのは以下であった。

- 生成ソフトウェア: `gpt-image` v2.0(OpenAI Media Service API)
- デジタルソース種別: `trainedAlgorithmicMedia`(AI生成物であることの宣言)
- 生成日時、C2PA UUID、XMP instanceID、SSL.com発行の署名証明書(サブジェクトは`OpenAI OpCo, LLC`)

**評価**

- **氏名・メールアドレス・アカウントID・GPS位置情報等の個人識別情報は一切含まれていない。** PII漏洩の観点では問題なし。
- 秘密情報(APIキー等)も含まれていない。
- ただし、**ロゴ(`icon.png`)を含む全画像がAI生成であることが、第三者にも技術的に検証可能な形で公開されている**点は認識しておくべきである。これは秘密情報の漏洩ではなく、事業上の開示判断の問題である。

**申し送り**

- 本サイトは「AI駆動開発のメタ事例」を掲げており(site-direction.md §2)、AI生成物の使用と方針の整合性はむしろ良好である。したがって**削除を推奨するものではない**。
- 一方で、C2PAメタデータは画像1点あたり数十KBを占めるため、パフォーマンス最適化(F-09)の観点からメタデータ除去を検討する場合がありうる。その際は「AI生成の事実を意図的に隠す」形にならないよう、事業方針(メタ事例としての透明性)と照らして判断すること。**セキュリティ上の必須対応ではない。**

## 10. 総合評価(v1.1)

| 重大度 | 件数 | 内訳 |
|---|---|---|
| 緊急 | 1件 | No.1(コミットメタデータ経由の個人情報公開。**未対応・要判断**) |
| 要対応 | 2件 | No.2(`.DS_Store`履歴残存。再発防止済み・履歴書き換えは要判断)、No.3(`.gitignore`不備。**是正済み**) |
| 参考 | 1件 | No.4(画像C2PAメタデータ。対応不要) |

- **v1.0で行われたドキュメントのプレースホルダー化は適切であり、置換漏れはない。**
- **APIキー・トークン・パスワード・接続文字列の直書きは、ワーキングツリー・Git履歴の全ブランチを通じて一切検出されなかった。**
- 一方で、v1.0では検査されていなかった**Gitメタデータ経路**から、ドキュメントで秘匿したのと同種のPII(個人のメールアドレス)がすでに公開状態にあることが判明した。これが本チェックにおける最重要の発見である。

## 11. 今後の申し送り事項(v1.1で更新)

### 11.1 実装フェーズ(developer)への申し送り
- Resendの**APIキー**は、いかなる場合もソースコード・ドキュメントに直書きしないこと。`.env.local`(ローカル)およびVercelの環境変数設定(本番)で管理する。**Vercelダッシュボードで設定した環境変数はリポジトリに入らないため、本番の秘密情報はここで管理するのが原則。**
- 受信先メールアドレスは`CONTACT_RECEIVE_EMAIL`のような環境変数名で設定し、コード・ドキュメントには変数名のみを記載する(要件定義書10章No.14)。
- `.env.example`を作成する場合は、**キー名のみを記載し、値は必ず空文字またはダミー値**とすること(`.gitignore`上、`.env.example`のみ追跡対象として許可している)。
- APIキーはクライアントサイド(ブラウザ)へ渡らない構成にすること。Next.jsでは`NEXT_PUBLIC_`プレフィックスを付けた環境変数はビルド成果物に埋め込まれ公開されるため、**Resend APIキーには絶対に`NEXT_PUBLIC_`を付けないこと。**
- Resend API呼び出しはServer Action / Route Handler等のサーバーサイドでのみ行うこと。
- エラーログ・デバッグログに、訪問者が入力したPII(氏名・メールアドレス・相談内容)やAPIキーを出力しないこと。特にVercelのFunction Logsは設定によっては閲覧範囲が広がるため注意する。
- コミット前に必ず`git status`で追跡対象ファイルを目視確認する。`git add .`の常用は避け、`git add <path>`で明示的に指定する運用を推奨する。

### 11.2 リポジトリ運用への申し送り
- **本リポジトリはpublicである。**コミットするすべての内容(コード・ドキュメント・画像・コミットメッセージ・コミットメタデータ)が公開されるという前提を、全フェーズの全担当で共有すること。
- 指摘No.1のGit設定変更(noreplyアドレス化)は、**次のコミットを行う前に実施することを強く推奨する。**実施しない限り、コミットのたびに露出が積み増される。
- 今後、設計書・実装コード・テスト結果報告書など新たな成果物が増えるたびに、コミット前にsecurity-expertによる同観点のチェックを実施すること。特に**実装フェーズ着手直後(Resend連携コードの初回コミット前)**は必須とする。
- 将来的にはgit-secrets / gitleaks / pre-commitフック等による自動チェックの導入も検討に値する(今回のイテレーションでは必須としない)。

### 11.3 対象外とした事項(v1.0から継続)
- お問い合わせフォームの入力内容(訪問者が任意に送信する会社名・氏名・メールアドレス等)自体は、本チェックの対象(開発側が管理するドキュメント・コードへの直書き)とは性質が異なるため対象外とした。
  - ただし**実装フェーズでは別途、当該入力内容の取り扱い(ログ出力の抑止、Resend経由での送信のみに限定し自前保存しない等)を設計上明確にすること**を推奨する。個人情報保護法上の取得・利用目的の明示(プライバシーポリシー掲載の要否)についても、公開前に事業判断として検討することが望ましい。

## 12. 変更履歴
| バージョン | 日付 | 実施者 | 内容 |
|---|---|---|---|
| v1.0 | 2026-09-09 | オーケストレーター | 初版作成。受信先メールアドレスの直書き問題を検出・是正(プレースホルダー化)。 |
| v1.1 | 2026-09-09 | security-expert | 正式チェックを実施しv1.0を検証・補強。v1.0の是正内容は適切と確認。新規に、①コミットメタデータ経由での個人メールアドレスの公開(緊急・未対応)、②`.DS_Store`の履歴残存(要対応)、③`.gitignore`の不備(要対応・**是正実施済み**)、④画像のC2PAメタデータ(参考)を検出。あわせてリモートがpublicである事実を明記。 |
| v1.2 | 2026-09-09 | オーケストレーター(クライアント判断に基づき対応) | 指摘No.1について、クライアントの判断(①Git設定はローカル+グローバル両方をnoreplyアドレス化する、②過去8コミットの露出は現状を受け入れ今後のみ対策する)を受け、`git config --local user.email` / `git config --global user.email` を`317452668+eiai00000001-cell@users.noreply.github.com`(GitHub付与のnoreplyアドレス)へ変更した。過去8コミットのメタデータおよび指摘No.2(`.DS_Store`履歴残存)は方針どおり是正対象外(現状受け入れ)とする。GitHub側の「Keep my email addresses private」「Block command line pushes that expose my email」設定は、GitHubアカウントの設定画面での操作が必要なためクライアント本人へのアクション項目として申し送る(13章参照)。 |

## 13. クライアントへの申し送り事項(v1.2時点で未完了のアクション)
- GitHubの `Settings → Emails` で以下2点を有効化することを推奨する(Claude Codeからは操作不可のため、本人による対応が必要):
  - 「Keep my email addresses private」
  - 「Block command line pushes that expose my email」(有効化すると、noreplyアドレス以外でのpushがGitHub側で拒否されるようになり、設定漏れによる再発を防げる)
- `.gitignore`の是正(指摘No.3)はワーキングツリー上で修正済みだが、本書作成時点で**未コミット**。次回のコミット時にあわせて取り込むこと。
